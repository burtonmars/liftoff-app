# Neon database connection string (from Vercel Storage → Postgres → .env.local snippet)
# Variable name is POSTGRES_URL when provisioned through Vercel dashboard
POSTGRES_URL=

# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET=
NEXTAUTH_URL=http://localhost:3000
